# Versioning (online)

Package version `0.2.0rc1` (PEP 440), marking this Special-Order freeze as a
release candidate for **eve-trader**. No git tag was actually created for
this identifier in this repo (`git tag -l` confirms none exist, 2026-09-26
audit) - despite this file and `docs/archive/PHASE_H_RELEASE.md` previously
claiming one (`v0.2.0-rc1`) did. Don't assume that tag exists on any other
clone/remote without checking there directly.

The previous package version was `0.1.0`. This RC certifies the Special-Order
path after Phases C–G; it is not a claim that Trading/Doctrine/Ore are
frozen.

The local desktop tool uses a separate identifier (`0.4.0rc1`).

After the tag: no production-code changes on this identifier.
