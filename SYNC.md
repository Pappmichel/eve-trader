# SYNC.md — keeping eve-trader (online) and eve-trader-local (offline) aligned

`eve-trader` (this repo) and `eve-trader-local`
(https://github.com/Pappmichel/eve-trader-local) are two separate codebases,
not a fork/branch pair — they deliberately diverge at the storage/config
layer (Postgres + RLS + `ConfigProxy` here vs. SQLite + a plain dataclass
there, see that repo's own `SYNC.md`/README). There is no tooling that
syncs them automatically. This file is the manually-maintained contract for
*what* needs porting by hand when it changes, and *what never should be*.

**In scope for syncing:** general business-logic/behavior — pricing
formulas, ESI/Goonmetrics parsing and math, classification rules, the OAuth
flow itself. **Out of scope, by design:** anything tenant-specific,
user-specific settings/data, or access-control/multi-tenant machinery — see
"Never sync" below. When in doubt: would a single local user's own settings
or a specific tenant's data ever appear in the diff? If yes, it's not a sync
candidate.

## Already ported

### Trading (plus the shared SDE/ESI/config foundation)

| File here | File in eve-trader-local | What's shared | Status |
|---|---|---|---|
| `eve_trader/auth.py` | `eve_trader_local/auth.py` | EVE SSO authorization-code+PKCE flow: state/PKCE generation, loopback callback handling, refresh logic | Ported 2026-09-02. Storage calls adapted (no tenant scoping), locking removed (single-process) — port algorithm changes only, not the storage glue. |
| `eve_trader/production/sde.py` | `eve_trader_local/sde.py` | Fuzzwork CSV fetch/parse: the file list, `_fetch_csv`'s retry-with-backoff, `_dump_etag`'s ETag freshness check, every row-shaping rule (the `_RELEVANT_ACTIVITIES` filter, the slot-defining dogma effect IDs, `metaGroupID`/`portionSize` handling) | Ported 2026-09-02, near-verbatim. **Data layer only.** Deliberately left out: `ProductionConfig.fuzzwork_csv_base` (a module constant there — no Production config exists in eve-trader-local yet), and most of this repo's `storage.py` SDE *read* helpers (`load_sde_*`, `get_type_category`, `get_blueprint_*`, `find_invention_recipe_candidates_*`, `get_type_slot`, `get_type_materials`, …) — those exist to serve Production/Doctrine/Refining business logic that isn't ported to eve-trader-local yet; port each one when the feature that needs it arrives there. Only `sde_row_counts`, `get_sde_type` and `search_sde_types` came over, to make its `refresh-sde`/`sde-status` CLI verifiable by a human. This repo's `lru_cache` memoisation on those reads was dropped on the local side (nothing there calls them in a hot loop yet, and a cache would need invalidation on every refresh). **Amended 2026-09-02**: the four SDE activity IDs are no longer inlined on the local side — `production/constants.py` is ported there now and owns them, same import direction as here (see the Production row below). |
| `eve_trader/esi_client.py` | `eve_trader_local/esi_client.py` | The whole generic ESI HTTP layer: `OrderStats`/`_summarize_orders`/`_percentile`, the retry-with-backoff core (`_get_response`/`_post_response`, `_retry_after_seconds`), ESI's `X-Esi-Error-Limit-*` budget tracking, `_get_all_pages`' page-1-then-concurrent-rest pagination, the class-level price/cost-index and short-TTL order-book caches, and every endpoint wrapper (character/corp orders, assets, wallet, industry jobs, blueprints, contracts, skills, structures, name/system resolution, market history, adjusted prices, cost indices) | Ported 2026-09-02, near-verbatim. `structure_order_stats_bulk_or_goonmetrics` followed 2026-09-02 with the `goonmetrics_client.py` port (see the row below) — it was held back only because it's the one method depending on that client. Adapted: this repo's `storage.with_current_tenant` wrapper around every `ThreadPoolExecutor.submit` is dropped on the local side (it exists purely because a worker thread doesn't inherit the tenant contextvar; there is no ambient tenant in eve-trader-local), and `ESIError` subclasses eve-trader-local's `ActionError` (this repo keeps them separate only to dodge a `config.py`↔`actions.py` circular import that doesn't exist there). `resolve_effective_volume`/`_bulk` needed two new SQLite-side helpers on the local side (`storage.get_type_category`, plus a `type_packaged_volume` cache table and its getter/setter) — that table is an ESI-only per-type constant, so it deliberately survives an SDE refresh there. |
| `eve_trader/goonmetrics_client.py` | `eve_trader_local/goonmetrics_client.py` | The current-price fetch shape (`appraise.gnf.lt/market/{slug}/prices.json`, its price parsing and retry-with-backoff) and the caching *pattern*: module-level cache dicts keyed by market, with a per-market lock rather than one shared lock, so a multi-second Jita fetch never blocks a concurrent home-market fetch that shares no cache entry with it. Plus `ESIClient.structure_order_stats_bulk_or_goonmetrics` (in `esi_client.py`, same as here) — the failsafe that falls back to a Goonmetrics snapshot when no seller is logged in or the real structure order book fails, returning `(stats, used_fallback)` with volumes zeroed. Plus the region price-history half: `price_history`/`price_history_chunked`/`HistoryPoint`/`_parse_history_xml`, its silent per-type ESI history fallback for when that no-SLA API is down (where `movement` passes ESI's `volume`, a unit count, straight through, never multiplied by average price), and the chunked fetch's per-chunk isolation so one bad chunk can't lose a whole multi-minute search. | Ported 2026-09-02 (current prices), completed 2026-09-02 with the price-history half. Both endpoints are now on the local side, and its module docstring spells out which question each answers — a region history average ("was this historically worth importing", asked against `reference_region_id`) is not interchangeable with a live current quote. Added there: `TradingConfig.goonmetrics_appraise_base` and `goonmetrics_history_base` (this repo hardcodes the former as a module constant `APPRAISE_BASE`; eve-trader-local already has an `esi_base` config precedent, so both became fields there) plus `chunk_size` (with a `_FIELD_RANGES` entry). |
| `eve_trader/candidate_discovery.py`, `eve_trader/models.py` | `eve_trader_local/candidate_discovery.py`, `eve_trader_local/models.py` | The whole discovery pass: `is_wanted_market_path`'s exclusion-prefix-only filtering (no keyword allowlist, no per-item m3 cap — an item is only ever dropped for failing on real profitability/volume later), `guess_category`'s real-SDE-category classification (with the string/volume heuristic kept strictly as the live-ESI-walk fallback, and the `IMPLANT_CATEGORY_ID`/`BOOSTER_GROUP_ID` split that keeps drugs from being labelled implants), `_market_group_path`, the SDE-cache-first/live-ESI-walk-fallback `build_candidate_universe`, and `build_focused_candidate_universe` (a deliberate pass-through). Plus the `Candidate` dataclass. | Ported 2026-09-02, near-verbatim on the local side — including issue #73 (capital-sized modules need their *packaged* volume, not the SDE flight volume) and issue #96 (resolve it in one bulk concurrent pass, never per-type inside the loop) context, which are real bugs, not stylistic notes. Added there to support it: `TradingConfig.excluded_path_prefixes`, four SDE read helpers (part of the set the `production/sde.py` row deliberately deferred), and `candidate_universe`/`focused_candidates` tables — persistence stays the *caller's* job there too (`actions.do_build_universe`), so the module itself stays free of storage writes. eve-trader-local's `load_candidate_universe` returns `Candidate` objects where this repo's `read_table` returns a pandas DataFrame (no pandas on the local side, and every caller there wants the dataclass). Only `Candidate` came from `models.py` in this row; its other dataclasses arrived with `history_backtest.py` (`NewCandidateResult`, see the row below) or still await `shortlist.py`/`trade_reconciliation.py`. |
| `eve_trader/history_backtest.py`, `eve_trader/models.py` | `eve_trader_local/history_backtest.py`, `eve_trader_local/models.py` | The whole backtest/scoring pass: `_index_history`, `_latest_margin`, `_score_candidate` (the landed/net_sell/margin formula, `score = avg_profit_m3 x log(1+avg_move) x hit_rate`, and the five-part `add` gate), `compute_margin_trends` (3-day vs 30-day rolling-margin momentum, with `MIN_TREND_HISTORY_DAYS` and the `MIN_BASELINE_MARGIN_MAGNITUDE` near-breakeven guard), `select_candidate_window`'s rotating coverage, and `find_new_import_candidates`/`_safe` with their per-batch isolation and incremental sinks. Plus the `NewCandidateResult` dataclass. | Ported 2026-09-02, near-verbatim apart from `compute_margin_trends`. **Pandas deliberately not added on the local side**: this repo's version takes a DataFrame, but everything it does with it is an inner join on `(type_id, date)`, a per-type grouping and two tail-window means — no vectorised math, no rolling/resampling, and at this size (a few thousand rows per candidate search) plain dicts/lists are just as fast. eve-trader-local's version takes an `Iterable[HistoryPoint]` instead, which is also the exact shape `find_new_import_candidates`' `history_sink` already emits there, so no DataFrame-shaped storage read has to exist for it. Same dependency-light call the `candidate_discovery.py` row already made. Added there to support it: `TradingConfig.safe_mode_max_ids`/`min_margin_threshold`/`min_hit_rate`/`min_avg_movement` (each with a `_FIELD_RANGES` entry). Deliberately left out on the local side: any storage coupling at all — persistence (`storage.save_goonmetrics_history`/`save_new_candidates`/`candidate_search_cursor` here) is the caller's job there too, same precedent as `candidate_discovery.py`, and the callbacks/offset that let a caller do it are ported; those three SQLite helpers arrive with eve-trader-local's future `do_find_new_candidates` equivalent, which doesn't have an actions layer yet. |
| `eve_trader/shortlist.py`, `eve_trader/models.py` | `eve_trader_local/shortlist.py`, `eve_trader_local/models.py` | The whole per-row evaluation: `_decision`'s precedence ladder (Inactive → Missing ID → No market data → Skip → Already ordered/Import, with "No market data" and "Skip" deliberately kept as two labels), `evaluate_shortlist_item`'s landed-cost/net-sell/margin/profit-per-m3 formula (priced off the *ask*, not the bid, with `jita_buy_broker_fee` still applied — deliberately conservative, not an oversight) including its "an inactive item is still fully priced" rule, `evaluate_shortlist`'s one-pass-over-pre-fetched-stats shape, `average_market_daily_volume`, `summary_counts`, `top_imports_by_daily_profit` and `audit_shortlist`. Plus the `ShortlistItem`/`ShortlistRow` dataclasses. | Ported 2026-09-02, near-verbatim on the local side. The two historical bugs this file's shape exists for (see this file's own "Theoretical ceiling" section) came over intact, in the code: **Profit / Day is `profit_per_unit x avg_daily_volume`** (real market-wide traded quantity from Goonmetrics region history for `reference_region_id`) — never `sell_volume`/order-book depth (issue #51) and never the trader's own realized sales alone (issue #100). An item Goonmetrics has no history for is excluded from Top Imports rather than estimated. `sell_volume` is still carried as "Listed Qty" and used only as the "is anything listed at all" gate in `_decision`. **Storage-ownership decision (local side): shortlist membership is persisted, but the persistence lives in `storage.py`, not `shortlist.py`** — this repo's `shortlist.py` imports no storage at all (every `storage.*` call is in `actions.py`), but eve-trader-local already keeps every table read/write in one module, so its `shortlist.py` port stays a pure, network-free, storage-free computation over values handed to it, same as its `candidate_discovery.py`/`history_backtest.py`. Added there to support it: `shortlist`/`shortlist_snapshot` tables with matching upsert/load/(de)activate/snapshot helpers (the snapshot read returns `ShortlistRow` objects where this repo's `latest_snapshot` returns a DataFrame — no pandas there), and `TradingConfig.min_profit_threshold` (with a `_FIELD_RANGES` entry bounded at 0 only, never at 1 — a target margin above 100% is legitimate, not a typo). Deliberately left out there: `shortlist_skip_streak`/`skip_grace_period_days`/`max_active_shortlist_items` — those belong to `do_refresh_and_prune_candidates`'s pruning policy in `actions.py`, not ported yet; they arrive with it. |

| `eve_trader/trade_reconciliation.py`, `eve_trader/models.py` | `eve_trader_local/trade_reconciliation.py`, `eve_trader_local/models.py` | The whole realized-P&L pass: `fetch_recent_transactions`' `from_id` cursor paging (a single un-paginated call only ever sees ESI's most recent 2500 transactions, which silently dropped older-but-still-in-window trades), `fetch_recent_journal_entries`' best-effort `market_transaction` lookup, `reconcile_realized_trades`' per-type FIFO match (pooled across every buyer/seller character, not paired 1:1) with both location filters and the `buy_date <= sell_date` rule, the landed-cost/net-sell formula including the wallet-journal real-tax path and its `_ASSUMED_TAX_RATE_IN_DEFAULT_HAIRCUT` back-out, the on-demand ESI type backfill, `summarize_realized` and `average_daily_sold_by_type`. Plus the `RealizedTrade` dataclass. | Ported 2026-09-02, near-verbatim on the local side. The two matching bugs this file's shape exists for came over in the code, not just in this table: **a buy dated after its sell is never a cost basis** (FIFO alone only orders buys chronologically; in this repo 20% of matched rows, and 21.8% of reported profit, had `buy_date > sell_date` — the matcher breaks and drops the sell's unmatched remainder rather than substituting a later buy), and **the buy side is region-filtered too** (it had no location filter at all, so any wallet transaction the buyer made anywhere could be paired against an unrelated structure sale). `_BUY_LOOKBACK_MULTIPLIER` (buys fetched over 3x the sell window) came with them. `average_daily_sold_by_type` is deliberately kept on the local side even though its shortlist no longer uses it: it answers the narrower "how much have *I* sold of this" question, not the market-wide Profit/Day figure that `shortlist.average_market_daily_volume` now answers there (issue #100 superseded issue #51's realized-sales version on the shortlist side, it did not delete this). **Storage-ownership decision: this module calls storage itself on the local side too, unlike `candidate_discovery.py`/`history_backtest.py`/`shortlist.py`** — this file itself imports `storage` and calls it directly in two places (`get_station_ids_in_region` for the buyer-side filter, and a `realized_trades` read in `average_daily_sold_by_type`), where `shortlist.py` imports no storage at all. So the local port follows this file's actual structure rather than the shortlist precedent: the calls live in `trade_reconciliation.py` there too, the SQL lives in eve-trader-local's one persistence module, and *writing* a run's results stays the caller's job exactly as here (`actions.do_reconcile_trades`). Added there to support it: a `realized_trades` table (replaced wholesale per run, not appended — a naked INSERT had bloated this repo's to ~19.8k rows over 13 runs) with matching save/load helpers (the load returns `RealizedTrade` objects where this repo's `read_table` returns a DataFrame — no pandas there), and `storage.get_station_ids_in_region` without this repo's `lru_cache` (nothing calls it in a hot loop there, and a cache would need invalidating on every SDE refresh). No config fields were needed on the local side: every field this module reads already existed. |

| `eve_trader/own_orders.py`, `eve_trader/models.py` | `eve_trader_local/own_orders.py`, `eve_trader_local/models.py` | All four live order-management checks: `fetch_own_sell_orders` (one pass over the character's orders producing `{type_id: volume_remain}` for open sells at `structure_id`, instead of one filtered query per shortlist row), `check_undercut`/`_pooled` (which of the seller's own listings a competitor has beaten on price), `fetch_seller_stock_without_order`/`_pooled` (shortlist stock physically at the structure with no sell order on it at all), and `fetch_buyer_already_covered` (items already on a buy order or already in inventory at Jita/the structure, so they needn't be imported again). Plus the `UnlistedStockRow`/`UndercutRow` dataclasses. | Ported 2026-09-02, near-verbatim on the local side. Two real constraints came over in the code, not just in this table: **"which orders are mine" can only be decided by `order_id`** — ESI's structure order book carries no owning-character field at all, so type_id/price matching would false-positive on a coincidentally identical competitor price; and **the own-order exclusion is pooled across every registered seller character** (issue #46 — several sellers share one structure's order slots, and a cheaper order from another of your own characters must never read as "undercut"). Same pooling applies to unlisted stock. This file's deliberate scope limits are kept as-is on the local side: only shortlist items are considered, only a *complete* absence of a sell order is flagged, and only assets sitting directly in the structure's own `location_id` are seen (anything nested in a container or parked ship carries that container's item_id). **Storage-ownership decision: this module calls storage itself on the local side too, following `trade_reconciliation.py`'s precedent rather than `shortlist.py`'s** — because this file itself imports `storage` and calls it directly (`get_station_ids_in_system`, for the buyer-side "is this asset in Jita" check), so the local port mirrors this file's actual structure; the SQL lives in eve-trader-local's one persistence module, and nothing in the ported module writes anything. Added there to support it: `storage.get_station_ids_in_system` (without this repo's `lru_cache`, same call already made for its other SDE reads) and `esi_client.NON_STOCK_LOCATION_FLAGS` — that constant lives in `storage.py` here because this repo's persisted asset tables filter on it; **on the local side it sits in `esi_client.py` instead**, since it describes ESI's asset payload and eve-trader-local has no persisted asset table for it to sit beside. Deliberately left out there: item-name resolution, the shortlist-item-id set, the margin/`sell_volume` enrichment and the "log in again for esi-assets.read_assets.v1" error message — all of those live in this repo's `actions.do_check_seller_unlisted_stock`/`do_check_undercut`, not in `own_orders.py`, and arrive with eve-trader-local's future action layer. |

### Production

A separate tool from Trading here too — the two share only the SDE/ESI/
config foundation. Production's backend is now fully ported on the local
side: classification, the constants table, the material-acquisition pricing
layer, the invention math, the buy-vs-build cost/margin core (ME/TE
stacking, job costs, recursive BOM expansion), `ProductionConfig`, build-
candidate discovery, the stock-aware planner (`plan_production`, with a
genuinely new local concept - `storage.stock_targets` - to feed it, plus its
invention-needs list and a real manual-stock ledger), the readiness-focused
`plan_asset_optimized`, `market_status`/`stock_value`, Special Orders
(`plan_special_order`, ad-hoc one-off build orders sharing that same BOM/
pricing core), the Margin page's ship browser (`discover_ship_margins`), and
the invention-location slice of the logistics helpers (`invention_logistics`/
`t1_bpc_invention_needs`) are all ported there. The multi-structure Logistik-
tab helpers (`logistics_status`/`distribution_recommendations`) are judged
genuinely out of scope for a single-structure local install rather than
deferred - see the `engine.py` row below for why.

| File here | File in eve-trader-local | What's shared | Status |
|---|---|---|---|
| `eve_trader/production/constants.py` | `eve_trader_local/production/constants.py` | The whole file: SDE activity IDs, `ACTIVITY_MODS`, `STRUCTURE_TYPES`/`RIG_TIERS` and `structure_rig_multiplier`/`rig_security_multiplier` (including `_rounded_security`'s CCP 1-decimal rule), `DECRYPTORS`, `SCC_SURCHARGE_RATE`, the meta-group IDs, `COMPONENT_GROUP_IDS`/job-category/ship-size group sets, and the job-slot skill IDs with `job_slots_from_skills`. All of it is fixed real-EVE game data with no config or storage dependency, so it is pure sync material — when CCP changes a bonus, both repos change identically. | Ported 2026-09-02, near-verbatim on the local side (in full, not just the parts `classify_activity` needs — the file is one flat table of constants and splitting it would only make the next Production port re-derive the split). **Activity-ID duplication decision:** eve-trader-local's `sde.py` used to inline `ACTIVITY_MANUFACTURING`/`REACTION`/`COPYING`/`INVENTION` with a comment saying it did so only because no Production module existed there to own them. That condition is now gone, so they have exactly one definition in `production/constants.py` there and `sde.py` imports them — the same direction as here, where `production/sde.py` imports them from `production/constants.py`. No cycle and no layering inversion on the local side either: this module imports nothing at all (not even `storage`), so it is a leaf the data layer can depend on. |
| `eve_trader/production/engine.py` | `eve_trader_local/production/engine.py` | `classify_activity`: the Reaction/Tech II/meta-group/Tech I decision ladder and the `meta_group_labels` mapping (metaGroupID 4/5/3/6 → Faction/Officer/Storyline/Deadspace). Plus (2026-09-02) the whole buy-vs-build cost core: `_material_qty`'s per-batch EVE rounding rule, the structure/rig profile split (`_is_component`/`job_category`/`_structure_profile`/`_structure_rig`/`_security_multiplier_for`), `_job_cost_rate`'s additive index×structure-bonus + facility-tax + SCC-surcharge formula with its three manual overrides, both ME/TE-stacking layers (`_activity_mods`, `_tech_ii_mods`' net-cost decryptor selection), `_unit_cost`/`unit_cost_detail`'s recursive best-of-buy-or-build with the EIV-priced job fee, `_buy_or_build_decision`, `_sell_margin`/`margin_home`/`margin_jita`, and the two price-agnostic BOM walks (`structural_material_closure`, `build_material_tree`). | Ported 2026-09-02, verbatim on the local side apart from cross-references. **This is the one file in the Production port whose reasoning must never be simplified away:** "Tech II" is decided *solely* by a real SDE invention-recipe lookup, never by `metaLevel` — this file's own `metaLevel >= 2` heuristic once mislabelled 854 of 4208 scanned manufacturable items as Tech II (every Faction/Pirate hull, Officer/Deadspace module and faction booster — Machariel/Nestor are metaLevel 8), pricing each off the flat Tech II ME/TE baseline instead of the Tech I one, while also *missing* genuinely invented Tech III subsystems (a Loki Core is metaLevel 1). Both halves are covered by explicit regression tests on the local side. No `ProductionConfig` was created there for classification alone: it's a pure function of the SDE cache with zero config dependency — the config class arrived with the first pricing/buy-vs-build port that actually needed a field (see the `production/config.py` row below). Added there to support classification: `storage.get_blueprint_for_product` and `storage.find_invention_recipe_candidates_by_product_type_id` (both read tables `sde.py` already populates there; this repo's `lru_cache` dropped, same call already made for every other SDE read on the local side). The `published = 1` filter on the blueprint lookup came with it — a leftover unpublished CCP test blueprint would otherwise make an unbuildable item look buildable — and has its own test there. **Amended 2026-09-02** with the cost/margin port above, near-verbatim on the local side too, including every real-EVE rule this file's comments exist for: material quantities are rounded up once per *batch* and floored at `runs` (a base_qty=1 material is never ME-reduced at all), the facility tax and SCC surcharge are *additive* terms and not folded into the cost-index multiplication (folding them in understated every job fee by ≥4.25% of EIV — a confirmed bug here), the job fee is `EIV × rate` off ESI *adjusted* prices at ME-0 quantities while material cost uses ME-reduced quantities at real market prices, and rig bonuses scale with system security off two different tables (Engineering Complex vs. reactor). **Amended again 2026-09-02** with the owned-BPO ME/TE preference, which is **no longer a divergence on the local side**: `_owned_bpo_mods` and `_activity_mods`' optional `blueprint_id` argument are both restored there, now that `production/esi_sync.py` populates `character_blueprints`/`corp_blueprints` on the local side and `storage.get_owned_bpo_best_me_te` can answer from real data. Tech I there now prices off the actual researched BPO — best ME and TE taken *independently* across every owned Original, copies excluded — and falls back to the flat perfect-research baseline only when the user doesn't own that blueprint or hasn't synced yet; Reaction and the four meta-group activities still never consult it there (a real EVE mechanic, not a fallback). Covered by a test asserting a changed *build cost*, not just a changed multiplier. Remaining divergences on the local side, all deliberate: **the bought-blueprint-copy cost term is left out** (`storage.get_manual_blueprint_copy_cost_per_run`, a manual user-entered table with no entry point there); **`_job_cost_rate`'s per-job-category cost-index refinement is left out** (issue #12 here — it reads the Logistik tab's per-category structure assignments, a web-UI concept with no local table, so `CostIndices` on the local side carries only the flat component/manufacturing split and the `category:` key never appears there); `component_system_name`/`manufacturing_system_name` are skipped with it (this repo resolves names to ids in a web-only Settings action; on the local side the id is configured directly). Two small structural changes on the local side: `_unit_cost`/`unit_cost_detail` share one `_material_cost_and_eiv` helper where this repo deliberately duplicates the formula (this repo's reason was to leave `_unit_cost`'s existing recursion and tests untouched, which doesn't apply to a fresh port there — and duplicating a formula whose whole point is that both callers price a build *identically* invites exactly the drift this repo's own comment warns about), and `_structural_material_closure` is public there (`structural_material_closure`) since its caller will live outside that module. `_build_margin` (the thin dispatcher choosing between `margin_home`/`margin_jita` by whether a stock target has a Jita target) is deferred there with the planner that owns stock targets; both margin functions themselves are ported. Added there to support it: `storage.get_system_security` (reads the `sde_solar_systems` rows `sde.py` already populates there; this repo's `lru_cache` dropped, same call already made for every other SDE read on the local side — this repo cached it for its planner's per-BOM-node hot loop, which doesn't exist there yet). **Amended 2026-09-02** with `discover_build_candidates`/`_scan_build_candidates` and `MAX_PLAUSIBLE_BUILD_MARGIN` on the local side — eve-trader-local's Production equivalent of Trading's `find-candidates`: scans every published, market-listed SDE item with a real recipe there and gates on `cfg.min_margin` (via `margin_home` — home-structure only, never Jita, matching this repo's own C-J-centric business model), then ranks survivors by `potential_daily_profit = daily_movement × margin × build_cost`, gated again by `cfg.min_daily_profit` — same theoretical-ceiling framing as Trading's own Profit/Day (see this file's own "Theoretical ceiling" section). **Deliberate simplification on the local side:** no `existing_target_ids` exclusion and `selected_decryptors` is always `{}` (no stock-target/manual-decryptor-override tables there yet — every Tech II/III candidate prices off whichever decryptor is cheapest in net terms). **Not cached there** — this repo's TTL/per-tenant-lock cache exists to serve concurrent web requests across tenants, irrelevant to a single local CLI user; a fresh scan runs on every call. `discover_ship_margins`/`_scan_ship_margins` is judged genuinely separate and left as a future candidate below. Added there to support it: `production/models.py`'s `BuildCandidate` dataclass and `ProductionConfig.min_margin`/`min_daily_profit`. CLI: `discover-build-candidates` (`--top-n`). **Amended 2026-09-03** with the stock-aware planner (`plan_production`) on the local side and its supporting `_current_stock`/`_base_runs`/`_parent_base_runs_for_buffer`/`_expand_all`, now that `storage.stock_targets` there gives it something to plan for; `_build_margin` (the home-vs-Jita dispatcher, previously deferred) is restored alongside it there, and `discover_build_candidates`'s `existing_target_ids` exclusion is now wired up on the local side too. **Real, deliberate simplification confirmed with the user, not a gap:** a stock target there is one target quantity plus a `jita_target` bool (feeding `_build_margin`'s dispatch, nothing else) — not this repo's three-way backup/home-market/Jita-market split with its own live sell-order-listing nets; and there is still no manual-stock override table on the local side, so `_current_stock` there is ESI-assets-plus-incoming-jobs only. `plan_asset_optimized` (this repo's second, readiness-focused variant) is judged genuinely separate and left unported there — it needs its own on-hand-only stock ledger the local side has no manual-stock table to make meaningfully different from `plan_production`'s own numbers yet; same for the invention-needs list and `market_status`/`stock_value`. Added there to support it: `storage.stock_targets` (`type_id, type_name, quantity, jita_target`) with matching CRUD, `production/models.py`'s `InventoryRow`/`BuyListEntry`/`BuildJobEntry` dataclasses (subsets of this repo's own), and `ProductionConfig.component_overbuild` (0.7 default, matching here). CLI there: `set-stock-target <item> <qty> [--jita]` / `remove-stock-target <item>` / `list-stock-targets` and `plan-production`. **Amended 2026-09-04, closing out this row entirely**: a real `manual_stock` table was added on the local side (confirmed against this repo's actual `storage.py` to be a genuinely separate user-maintained on-hand count layered on top of ESI-synced assets, not a simplification of them - same "simplest useful shape" precedent `stock_targets` itself set), folded into both `_current_stock` and a new `_stock_on_hand`. `plan_asset_optimized` is ported on the local side too: the readiness-focused second Bauliste, splitting "physically on hand right now" (`_stock_on_hand`) from "current_stock incl. incoming jobs" (`_current_stock`, for shortfall sizing) via `_allocate_scarce_stock`'s smallest-claim-first allocation. **Deliberate simplification there**: `AssetPlanJob.recommended_slots` and its job-slot-allocation machinery are left out - it needs live character-skill ESI data plus a per-character planning-exclusion flag, and the local `esi_sync.py` deliberately never requests skill scopes at all (no planner existed yet to need them); `runs_ready_now`/`job_runs` alone is still a useful readiness signal there. The invention-needs list inside `plan_production` is ported there too (via newly-ported `storage.available_blueprint_copies`/`get_blueprint_time`, verbatim including this file's own confirmed bug fixes - runs summed, not copies counted). `market_status`/`stock_value` are ported on the local side as genuinely simplified reads (no separate market-listed-volume column, since the local stock target has no distinct market-target column) rather than full three-way parity. `discover_ship_margins`/`_scan_ship_margins` (the Margin page's ungated ship browser) are ported near-verbatim there too - every published Ship-category item with a real recipe, no `min_margin`/`min_daily_profit` gate, no history lookup. The logistics/distribution helpers were investigated individually rather than as one block, per this file's real per-category-structure-assignment dependency: **`logistics_status`/`distribution_recommendations` are judged genuinely out of scope on the local side, not deferred** - both are fundamentally about routing material between *several* physical structures a solo player wouldn't realistically operate, and the local config model (one `home_location_id`, a flat two-way system split) has no multi-structure routing concept for them to attach to. **`invention_logistics`/`t1_bpc_invention_needs` are ported there, in reduced single-location form** - this file's own `invention_location_id` was already just one field, the same shape `home_location_id` already has locally, so `ProductionConfig.invention_location_id` (new there) is a legitimate single-field port; the multi-location `pull_from_location_id`/`pull_from_available` hints are dropped with the rest of the Logistik-tab concept. `_direct_material_mult` (only feeds `logistics_status`) is left out with it. **Amended 2026-09-03** with `plan_special_order` on the local side (ad-hoc, one-off build orders sharing that same BOM/pricing core): reuses `plan_production`'s own already-ported internals directly (`_base_runs`/`_expand_all`/`_current_stock`/`structural_material_closure`) rather than duplicating them there - the only shared-code change needed was a new `ignore_current_stock` parameter on `_expand_all` (the "from scratch" mode) and factoring the inline buy_list/build_list construction into `_build_buy_list`/`_build_build_list` helpers so both planners build the same row shape from the same inputs, matching this repo's own equivalent split. Two deliberate differences from `plan_production` on the local side too, matching this repo's confirmed behavior: no margin gate (an order must be fulfilled regardless of `cfg.min_margin`) and `net_against_stock`'s `stock_overlap_warning`. No invention-needs list and no manual-stock override table there either, same already-documented gaps as `plan_production` itself. Storage there: `special_orders`/`special_order_items` ported from this repo's `docs/special_orders_schema.sql` minus `tenant_id`/RLS, same Python-UUID convention the Doctrine tables already use. CLI there: `create-special-order <item:qty> [...] [--note] [--net-against-stock]` (accepts a name or type_id per item, resolved the same way `set-stock-target` does - this repo's own `do_create_special_order` takes pre-resolved type_ids only, fed by a frontend item picker with no CLI equivalent on the local side) / `list-special-orders` / `update-special-order` / `remove-special-order` / `compute-special-order`. |
| `eve_trader/production/config.py` | `eve_trader_local/production/config.py` | The `ProductionConfig` **field definitions** that are ported (defaults included), and the `validate_production_overrides` enum layer: the structure/rig fields are checked against `production/constants.py`'s `STRUCTURE_TYPES`/`RIG_TIERS`, on top of the generic type/range checks, because a hand-edited config.yaml bypasses any dropdown and a typo'd structure name would otherwise pass the plain string check and blow up later as a bare `KeyError` inside the build-cost math. | Ported 2026-09-02 on the local side. **Deliberately a subset there.** Ported: `jita_buy_broker_fee`, `haul_cost_per_m3`, `home_market`, `home_location_id` (all four read by `pricing.py`), the three invention skill levels (`encryption_skill_level`/`datacore_skill_1_level`/`datacore_skill_2_level`, read by `invention.skill_multiplier`, each with a `_FIELD_RANGES` entry bounded 0-5 — EVE's own hard skill-level ceiling, not an app choice), plus the six `*_structure_type`/`*_rig_tier` fields — those have no `pricing.py` consumer yet, but `constants.structure_rig_multiplier` (already ported) is the function they exist to feed, and they are the entire reason the enum-validation layer exists, so porting the layer without its fields would have been half a port. **Amended 2026-09-02** with the fields `engine.py`'s cost/margin port reads on the local side: `market_fees` (margins), `facility_tax_rate` and the three `*_cost_index_override`s (`_job_cost_rate`, all four range-checked in the shared `config.py` the same way there), and `component_system_id`/`manufacturing_system_id` (the 2-way system split `_job_cost_rate`/`_security_multiplier_for` resolve their cost index and rig-security multiplier from). Still deferred there, each with the module that will need it: `component_overbuild`/`bpc_inventory`/`min_margin` (the stock-aware planner), `min_daily_profit` (`discover_build_candidates`), `component_system_name`/`manufacturing_system_name` (this repo resolves those to the `_id` fields in a web-only Settings action; a local install sets the id directly), `distribution_source_location_id`/`invention_location_id` (logistics tabs, web-UI-shaped), and `fuzzwork_csv_base` (already a module constant in eve-trader-local's `sde.py`). **Layering, matching this repo's own:** the shared `config.py` must never import this module on the local side — `validate_config_overrides`/`apply_config_overrides` are reused from it, and the Production-only enum checks are layered on top there, since they need `production/constants.py`. The one exception is the `_FIELD_RANGES` entries for `haul_cost_per_m3`/`home_location_id`, which live in the shared `config.py` there exactly as they do here: `_check_range` is keyed by field *name* and needs no Production import to do it. Storage scope key is `"production"` there (this repo's own), so Trading and Production overrides can never collide. No `ConfigProxy`/contextvars there: single user, one config. **Amended 2026-09-02** with `min_margin` (0.15 default) and `min_daily_profit` (0.0 default), both range-checked `(0, None)` in the shared `config.py` on the local side — added for `engine.discover_build_candidates` there rather than the stock-aware planner this repo originally scoped `min_margin` to: eve-trader-local has no stock-target table yet for a planner's own copy of that gate to attach to, so one `min_margin` field does both jobs there until the planner exists. **Amended 2026-09-04** with `component_overbuild` (0.7 default, for the planner's overbuild-buffer math) and `invention_location_id` (a single "where do you invent" location, the same shape `home_location_id` already has there - unlike `distribution_source_location_id`, not a multi-category Logistik assignment, so it left the deferred list; see the `engine.py` row's own reasoning), both range-checked on the local side. Still deferred there: `bpc_inventory`, `component_system_name`/`manufacturing_system_name`, `distribution_source_location_id` (a genuine multi-structure Logistik concept - see the `engine.py` row for why `logistics_status`/`distribution_recommendations` are judged out of scope entirely there), and `fuzzwork_csv_base`. |
| `eve_trader/production/esi_sync.py`, `eve_trader/production/actions.py` (`do_sync_esi`) | `eve_trader_local/production/esi_sync.py`, `eve_trader_local/production/actions.py` | The producer-character ESI sync: `PRODUCTION_SCOPES`, `list_producer_characters`, the three row builders, `_normalize_activity_id`, `_fetch_character_data` and `sync_esi`'s two-phase shape — a parallel per-character Phase A (independent, latency-bound) and a deliberately *sequential* Phase B for corp-level data, which is stateful across characters (each corp fetched once, retried with the next character sharing it when an earlier one lacks Director). Plus its failure contract: every ESI call is individually isolated so one missing scope or role reports as a skipped character/corp instead of aborting the sync. | Ported 2026-09-02 on the local side. Two real EVE/ESI quirks came over verbatim because nothing else in either repo encodes them: **live industry jobs report Reactions as activity_id 9 while the SDE files every reaction recipe under 11** (normalized once at ingestion on the local side too, so every downstream SDE lookup keeps working — has its own test there), and **ESI's blueprint sentinels**, `runs = -1` for an Original and `quantity = -2` for a copy, which is what makes `get_owned_bpo_best_me_te`'s BPO-only filter correct rather than arbitrary. **Adapted, not ported: this repo's `storage.with_current_tenant` wrapping of the thread pool has no analogue on the local side** (no tenancy there, and `storage.connect()` already opens a fresh connection per call, so nothing is shared across threads) — but this repo's *reason* for it, a worker thread refreshing an expired token, still bites there: eve-trader-local's `TokenManager` deliberately has no refresh lock. So `_usable_characters` refreshes every token up front on the calling thread there and drops the ones that fail, which both keeps refresh off the pool and turns a dead refresh token into a reported "skipped" character. **Deliberately narrower than this repo's `sync_esi` on the local side:** the sell-order, character-skill/job-slot and structure-name-discovery phases are left out there with the features that consume them (Trading's `own_orders.py` already covers live sell orders there; job slots only feed the not-yet-ported planner; structure-name discovery feeds this repo's web-only Logistik per-category structure assignments) — so `PRODUCTION_SCOPES` there asks only for what is actually called, plus `esi-markets.structure_markets.v1` for `pricing.py`'s home order book. Added there to support it: six snapshot tables (`character_`/`corp_` × `assets`/`blueprints`/`industry_jobs`), `storage.replace_assets`/`replace_blueprints`/`replace_industry_jobs` with this repo's `resolved_location_id` container-chain walk (`_resolve_locations`; resolving only one level was a confirmed bug here that made stock inside a container inside a corp hangar read as absent), and their three readers `esi_stock_at_location`, `esi_incoming_industry_qty` and `get_owned_bpo_best_me_te`. `PRODUCER_ROLE_PREFIX` moved on the local side from `pricing.py` to `esi_sync.py`, matching this repo's own direction (`pricing.py` reaches into `esi_sync` for it here). `production/actions.py` is new on the local side (mirroring this repo's own Trading/Production actions split) and holds `do_sync_esi`/`do_get_esi_sync_time`/`do_list_producer_characters`/`do_remove_producer_character`; this repo's two cache invalidations in `do_sync_esi` have no local counterpart yet (neither cache exists on the local side). CLI there: `sync-esi`, and `auth --role producer` picks `PRODUCTION_SCOPES`. |
| `eve_trader/production/invention.py`, `eve_trader/production/models.py` | `eve_trader_local/production/invention.py`, `eve_trader_local/production/models.py` | The whole invention calculation: `skill_multiplier` (EVE's real formula — each of the two datacore/science skills is worth 1/30 per level and the encryption skill 1/40, all three *added*, never multiplied together), `estimate`'s per-attempt datacore/decryptor/relic pricing with the 100%-probability cap and the `max(0, base_runs + run_bonus)` output-run count, `reducible_material_cost`'s qty>1 rule, and the four selection helpers (`compare_decryptors`/`best_decryptor_for_item` for one fixed invention source, `compare_recipes_and_decryptors`/`best_recipe_and_decryptor` across every source, and `best_recipe_for_decryptor` for a hand-picked decryptor with an auto-optimised source). Plus the `InventionResult` dataclass. | Ported 2026-09-02 on the local side, near-verbatim. Three real decisions came over in the code there, not just in this table: **the ranking figure is net cost per BPC run**, i.e. `(attempt cost / probability) / output_runs - (ME/100) * reducible_material_cost_per_run` — a decryptor's ME is worth real ISK on every subsequent build, so ranking on raw invention cost alone picks the wrong decryptor; **an input with no sell order anywhere is "unknown", never 0 ISK** (`all_prices_known` gates `expected_cost_per_success`/`_per_run`/`net_cost_per_run` to `None`, so a missing price can't silently make an expensive decryptor look cheap — issue confirmed here 2026-08-29); and **a Tech III Sleeper relic IS priced into the attempt cost while a T1 blueprint deliberately is not** (a relic must be bought/looted fresh per attempt, a T1 BPC is a near-free reprint of an owned BPO — omitting the relic overstated Tech III profitability, reported by a user 2026-08-30). Tech III's up-to-three relic grades (Intact/Malfunctioning/Wrecked, materially different odds *and* output runs *and* price) are compared side by side rather than always assuming the best-odds grade. Adapted on the local side: this repo's bare `ValueError` for "no recipe/no probability row" is a named `NoInventionRecipe(ActionError)` there — the multi-candidate helpers catch it to skip an unusable candidate, so it has to be narrower than a bare exception, and it still prints as a plain message if it reaches the user; an unknown decryptor name raises `ActionError` there instead of a bare `KeyError`. Added there to support it: `storage.get_invention_recipe` and `storage.get_blueprint_materials` (both read tables `sde.py` already populates there; this repo's `lru_cache` dropped, same call already made for every other SDE read on the local side — this repo added it here purely for its own planner's ~12-queries-per-item hot loop, which doesn't exist there yet), a new `production/models.py` (mirroring this repo's own Trading/Production dataclass split), and `ProductionConfig`'s three invention skill fields. This repo's job-installation-fee and T1-BPC-copy-cost omissions are kept as-is on the local side too and documented in the module docstring — they're consistent across every decryptor for the same item, so they don't change *which* one is recommended except in genuinely marginal calls. |
| `eve_trader/production/pricing.py` | `eve_trader_local/production/pricing.py` | The material-acquisition half of buy-vs-build: `home_prices`/`jita_prices` (ESI order-book first, Goonmetrics current-price snapshot only as a lazy fallback), `_candidate_prices`/`buy_source`/`buy_price` (home sell price vs. Jita sell price + haul cost, both inflated by the buy-side broker fee, only the Jita side hauled) and `system_cost_indices_for`. | Ported 2026-09-02 on the local side. The two decisions worth keeping in sync are both in the code there: **the Goonmetrics fallback is only ever fetched on an actual ESI failure**, never eagerly alongside the ESI attempt (the success path would otherwise pay for a multi-megabyte download it throws away, and an unset `home_location_id` would still hit the network), and **`buy_source` and `buy_price` share one `_candidate_prices` helper** so the reported source can never disagree with the reported price. A zero sell price means "nothing listed", never "free" — covered by its own test there, since `region_order_stats_bulk` reports an unlisted type as an empty `OrderStats`. Adapted, not ported: this repo resolves producer characters via `production/esi_sync.list_producer_characters()`; on the local side it is `TokenManager.list_records("producer")`, eve-trader-local's own role-prefix convention (a `producer` role, deliberately not Trading's `seller` — the character who can dock at the industry structure isn't necessarily the one running sell orders). Left out there: this repo's `production/jita_price_cache` hourly snapshot (a pure perf layer for its web `plan_production` path; nothing on the local side calls this in a loop yet — port it with the planner if the same cost shows up). Jita's region id still comes from `TradingConfig` on the local side too, not `ProductionConfig`, matching here. Source labels are `"home"`/`"jita"` on the local side where this repo returns `"C-J"`/`"Jita"` — this repo's are its own structure's nickname baked into a return value; anything that switches on them must be ported with that in mind. Broad `except Exception` narrowed to `(ActionError, requests.RequestException)` on the local side — `ESIError` subclasses `ActionError` there, and a token that can no longer be refreshed raises `ActionError` too. |

### Doctrine

A separate tool from Trading/Production on the local side too - the three
share only the SDE/ESI/config foundation. Only the EFT-fitting parser and
its supporting constants/dataclasses are ported there so far; matching/
validation against a synced contract, stockpile/deviation scoring, ESI
contract sync and the orchestration layer (`actions.py`/`engine.py`) are
all still "Candidates" below.

| File here | File in eve-trader-local | What's shared | Status |
|---|---|---|---|
| `eve_trader/doctrine/constants.py` | `eve_trader_local/doctrine/constants.py` | The whole file: the nine EFT slot sections plus the local `fuelbay`/`shipmaintenancebay` addition (issue #18), `EXACT_SECTIONS`/`CONSUME_SECTIONS`, the SDE category IDs (`SHIP_CATEGORY_ID`/`CHARGE_CATEGORY_ID`/`DRONE_CATEGORY_ID`/`STRUCTURE_CATEGORY_ID`/`FIGHTER_CATEGORY_ID`/`VALID_HULL_CATEGORY_IDS`), the parse-issue/deviation/severity/validation-status/ampel vocabularies, the ESI contract-sync status sets, and the B.5 matching weights/threshold. All of it is fixed, curated business data with no config or storage dependency, so it's pure sync material. | Ported 2026-09-03 on the local side, verbatim (in full, same "one flat table, don't split it" reasoning `production/constants.py`'s own row uses). Deliberately duplicates `SHIP_CATEGORY_ID` there rather than importing it from `production/constants.py` - matching this file's own documented choice (see its module docstring) to avoid a doctrine<->production dependency edge that doesn't otherwise need to exist. |
| `eve_trader/doctrine/models.py` | `eve_trader_local/doctrine/models.py` | The whole file, ported in full rather than split on the local side - same reasoning as `constants.py`'s row above. Only `ParsedItem`/`ParsedIssue`/`ParsedFitting` (`parser.py`'s actual return shape) and the master-data shapes (`Doctrine`, `Fitting`, `FittingItem`, `ParseIssue`) have a consumer there; the sync/results and status/ampel dataclasses (`ContractRow`, `StockpileRow`, `FittingStatus`, ...) came along for the same "don't re-derive the split later" reason and wait for the module that will actually build them. | Ported 2026-09-03 on the local side, verbatim apart from the module docstring, which no longer claims an `api/schemas.py` Pydantic mirror there (eve-trader-local has no web API layer at all). |
| `eve_trader/doctrine/parser.py` | `eve_trader_local/doctrine/parser.py` | The whole EFT fitting-format parser: `parse_fitting`'s full grammar (header/body/blocks, `_EMPTY_MARKER_RE`/`_OFFLINE_SUFFIX_RE`/`_QTY_SUFFIX_RE`, the position-vs-SDE "gegenprobe" gated on `has_markers`), `_resolve_names`' right-to-left comma-split disambiguation, `_classify_section`'s qty-suffix-overrides-SDE-slot rule, `_levenshtein`/`_suggest`'s "did you mean" hull/item-name suggestion (real fuzzy matching is never used for actual resolution, only for a failed-lookup hint), and `parse_bay_items` (issue #18's Fuel Bay/Ship Maintenance Bay plain-text lists, entered outside the main EFT grammar since CCP's own Fitting Formats spec has no syntax for either). Plus the `ResolvedType` dataclass and `FittingParseError`. | Ported 2026-09-03 on the local side, verbatim - genuinely pure, storage-free text parsing: every SDE lookup arrives as an injected `resolve_name`/`resolve_slot` callable, so the module itself imports nothing beyond `.constants`/`.models`. **`validation.py` coupling, checked and confirmed clean**: `doctrine/validation.py` (not ported) consumes `FittingItem`/`ContractItemRow`/`DeviationRow` - `parser.py`'s *output* type, one step downstream - and neither module imports the other; `parser.py` produces structured data, `validation.py` is a wholly separate later pass over already-parsed/already-synced rows. Porting the parser alone left nothing half-working there. Added there to support real (non-test-fake) resolver wiring: `storage.get_type_slot`, `storage.resolve_sde_type_by_name` and `storage.list_hull_type_names` (all three pure SDE reads over tables `sde.py` already populates on the local side - `sde_type_slots`/`sde_types`/`sde_groups` - with no tenant/config dependency; this repo's `lru_cache` on `get_type_slot` dropped, same call already made for every other SDE read there). The real wiring these resolvers exist for (`doctrine/engine.py`'s `_resolve_name`/`_resolve_slot`/`parse_fitting_text`) is itself still a "Candidate" below there - CLI: `parse-fitting <path>`, a standalone smoke-test command (no `engine.py`/`actions.py` exists there yet to own the wiring, so it's inlined in `cli.py`). |
| `eve_trader/doctrine/esi_sync.py`, `eve_trader/doctrine/engine.py` | `eve_trader_local/doctrine/esi_sync.py`, `eve_trader_local/doctrine/engine.py` | The real-SDE resolver wiring (`_resolve_name`/`_resolve_slot`/`parse_fitting_text`/`parse_bay_items_text`), Fitting/Doctrine row<->dataclass mapping, `load_match_candidates`/`match_and_validate_contract`'s per-contract matching (with the `NO_HULL_MATCH` sentinel never persisted), `stockpile_rows_for_doctrine`/`aggregate_stockpile_rows`' Soll/Ist/allocation orchestration, and `fitting_status`/`doctrine_status`'s ampel rollups - plus the ESI contract-sync flow itself: `_passes_prefilter`/`_issued_by_own_identity`'s pre-items-fetch filtering, the "carry forward an unchanged contract's already-fetched items" optimization, and the asset sync (Stockpile's Ist side). | Ported 2026-09-03 on the local side. **Deliberate structural simplification there, confirmed by reading this repo's actual role split rather than assumed**: this repo keeps contract-reading characters (`DOCTRINE_ROLE_PREFIX`/`DOCTRINE_SCOPES`) and asset-scanning characters (`DOCTRINE_ASSET_ROLE_PREFIX`/`DOCTRINE_ASSET_SCOPES`) as two entirely separate character-auth groups, reasoned here as letting a multi-tenant Doctrine-only tenant use Stockpile without ever touching Production, and as not asking a contracts-only character for asset scopes it doesn't need. Neither reason survives contact with a single-user local install - so the local side merges both into one `DOCTRINE_ROLE_PREFIX`/`DOCTRINE_SCOPES` pair and one `sync_doctrine()` entry point (`sync_contracts`/`sync_assets` still exist underneath there, independently callable and independently isolated). `fitting_status`'s `multibuy_cost` is therefore always `None` there, never wired to a live home-price lookup. **Update 2026-09-03**: issue #19's `doctrine_contract_history` (a permanent, append-only "who bought what and when" log, independent of `doctrine_contracts`' own active/wholesale-replaced snapshot) is now ported too on the local side - `esi_sync.sync_contracts` there additionally scans the same already-fetched raw contract lists for `FINISHED_CONTRACT_STATUSES` (`_passes_history_filter`), reuses whichever `fitting_id` that contract was already matched against while still outstanding (never re-matched from items, which are immutable once finished), and upserts one denormalized row (fitting_name/hull_type_id snapshotted at record time, so the row stays meaningful even after that fitting is later edited/deleted) per newly-finished contract, keyed by `contract_id` alone (single-user there, no tenant-collision PK widening needed). Added on the local side to support it: the nine `doctrine_*` tables (`storage.py`, ids are Python `uuid.uuid4()` strings rather than Postgres's `gen_random_uuid()`) with their full CRUD, plus `storage.esi_stock_at_location`'s new `tables` parameter (defaulting to Production's own asset tables, so that port's callers are unaffected) so Doctrine's stockpile computation there can point it at its own independent asset-table set - Doctrine keeps its own independent synced-asset cache on the local side too, exactly like here, so Stockpile works even if Production was never set up. `DoctrineConfig` is new on the local side: `doctrine_structure_id`/`stockpile_location_id` (each falling back to `TradingConfig.structure_id`), `cargo_tolerance_pct`, `strict_extras` - loaded/saved exactly like `ProductionConfig` there (defaults -> config.yaml -> stored overrides, scope key `"doctrine"`). CLI there: `auth --role doctrine`, `create-doctrine`/`list-doctrines`, `add-fitting`/`list-fittings`, `sync-doctrine`, `validate-contracts`, `doctrine-status`/`stockpile-status`, `shopping-list`, `list-contracts`, `contract-history`. **Update 2026-09-03**: the Shopping List's Build-vs-Buy-C-J-vs-Buy-Jita comparison (`shopping_list_rows`/`_shopping_prices`) is now ported too on the local side, reusing Production's real cost engine directly (`production/engine.py`'s `unit_cost_detail`/`_haul_volume`/`structural_material_closure`, `production/pricing.py`'s `home_prices`/`jita_prices`) so a user's owned-BPO/ME/TE/structure config gives the exact same build-cost number there as in Production itself - same reasoning as this repo's own import comment in `doctrine/engine.py`. One deliberate simplification on the local side: no separate live-order-book presence check (this repo's issue #10 fix) - `home_prices`/`jita_prices` there are themselves already ESI order-book-first (unlike this repo's Goonmetrics-first shape those functions had when issue #10 was filed), so the bug that check exists to catch can't reach this function on the local side in the first place. `DoctrineConfig.import_cost_per_m3` is new there, deliberately independent of `ProductionConfig.haul_cost_per_m3`, matching this repo's own field. |
| `eve_trader/doctrine/actions.py` | `eve_trader_local/doctrine/actions.py` | The Doctrine-relevant `do_*` subset: `do_create_doctrine`/`do_update_doctrine`/`do_delete_doctrine`/`do_list_doctrines`, `do_parse_fitting`/`do_add_fitting`/`do_update_fitting`/`do_delete_fitting`/`do_list_fittings`/`do_get_fitting_detail` (with issue #18's `_merge_bay_items` line-numbering), `do_sync_doctrine`, `do_validate_contracts`, `do_get_doctrine_status`/`do_get_stockpile_status`/`do_list_contracts`, and `do_update_settings`. Same thin-orchestration/`ActionError`-boundary shape as `actions.py`/`production/actions.py`. | Ported 2026-09-03 on the local side. Deliberately excluded there, matching `engine.py`'s own deferrals: `do_sync_assets`/`do_get_asset_sync_time` as separate actions (folded into `do_sync_doctrine`), `do_contract_history` (issue #19, not ported), and `do_get_shopping_list` (needs the Shopping List engine code, not ported). `do_list_doctrine_characters`/`do_remove_doctrine_character` are kept on the local side even though there's no dedicated Doctrine CLI command for them yet (`auth --role doctrine`/`whoami` already cover login/listing) - cheap, and a future GUI's character-management view wants them directly. `main()` was also fixed there to call `production.config.reload()` on every invocation, alongside the new `doctrine.config.reload()` - a pre-existing gap (Production Settings overrides were never actually re-applied on a fresh CLI process before this) that would have silently affected Doctrine's own settings too if left alone there. |
| `eve_trader/doctrine/validation.py` | `eve_trader_local/doctrine/validation.py` | The whole file, verbatim: B.2/C.1 Soll construction (`build_contract_soll`'s exact/consume split with the charge-floor-of-1 merge, `build_stockpile_soll`'s hull-included multiset times `total_sets` including issue #36's additive contract-shortfall demand), B.2/B.3 the hull-gate/Ist multiset (`hull_gate_satisfied` deliberately not requiring `is_singleton` - a real fitted ship's contract-items response isn't a reliable packaged-vs-assembled signal, unlike the asset endpoints; `build_contract_ist` summing separately-stacked damaged charges, since ESI's contract-items schema has no damage field to filter on at all), B.4 `compute_deviations`'s full missing/short/extra table plus `is_included=False` "requested consideration" extras, B.5 `match_score`'s weighted multiset-overlap and `clears_match_threshold`, B.6 `pair_wrong_variants`' same-group/same-slot greedy re-labeling, B.7 `contract_status`, C.2/C.3 `allocate_stockpile`'s greedy priority allocation and `stockpile_deviation`, and B.8's `contract_ampel`/`stockpile_ampel`/`worst_ampel`/`worst_severity` aggregation. | Ported 2026-09-03 on the local side, verbatim apart from the module docstring (no `engine.py` exists there yet to be its caller - noted as currently caller-less beyond its own test suite; confirmed cleanly separable from `parser.py`, same finding the parser row above already recorded). No new SDE reads or `DoctrineConfig` fields were needed there - every input (`FittingItem`/`ContractItemRow`/`DeviationRow`, plus plain scalars like `cargo_tolerance_pct`) is already-loaded data the caller supplies; `pair_wrong_variants` takes `group_id_of`/`slot_of` as injected callables, same "caller does the real lookup" shape as `parser.py`'s own resolvers. Storage-free by construction, matching this file's own design constraint (module docstring), not a repo-specific choice on the local side - nothing to decide there. |

### Ore & Minerals

A separate tool from Trading/Production/Doctrine here too (issue #90) - the
four share only the SDE/ESI/config foundation. On the local side: the fixed
reprocessing-yield constants, the actual ore/ice + scrapmetal yield math,
the inventory-paste parser (issue #92), both live-pricing quote calculations
(the Ore Shortlist's per-row profit math, issue #91, and the Reprocessing-
tab's sell-as-is-vs-refine recommendation, issue #92), the candidate
discovery universe, and the Mineral Shopping List's LP optimizer (issue #93)
are all ported there. Only the orchestration `do_*` layer is still on the
"Candidates" list below.

**Dependency note**: eve-trader-local's `refining/optimizer.py` is the one
module in that repo that needs anything beyond `requests`/`PyYAML` - it adds
**`scipy`** (and `numpy`, which comes with it) to its `pyproject.toml`, for
`scipy.optimize.linprog`'s HiGHS solver. A deliberate, one-off exception to
that repo's dependency-light discipline (the same call made here for the
same module): the shopping list is a genuine linear program, and hand-
rolling a simplex implementation is notoriously easy to get subtly wrong.
Nothing else there imports scipy, and nothing else should without the same
justification.

| File here | File in eve-trader-local | What's shared | Status |
|---|---|---|---|
| `eve_trader/refining/constants.py` | `eve_trader_local/refining/constants.py` | The whole file, verbatim: `BASE_YIELD_POINTS`/`STRUCTURE_YIELD_MODIFIER`/`RIG_YIELD_BONUS_POINTS`/`REPROCESSING_IMPLANT_BONUS`/the three per-level skill-bonus constants (the ore/ice path), `SCRAPMETAL_BASE_YIELD`/`SCRAPMETAL_SKILL_BONUS_PER_LEVEL` (the scrapmetal path), the compression constants (`ORE_ICE_CATEGORY_ID`/`ICE_GROUP_IDS`/`COMPRESSED_VOLUME_RATIO_*`), `_rounded_security`/`security_yield_modifier`/`clamp_skill_level` and the three `*_options()` helpers. All fixed, curated real-EVE game data with no config or storage dependency, so it's pure sync material. | Ported 2026-09-03 on the local side, verbatim apart from two docstring cross-references (see the reprocessing.py row below - the local side folds this repo's separate `refining/engine.py` into its own `reprocessing.py`, so the two "see refining/engine.py" pointers were updated to name that file instead; the formulas/values themselves are untouched). Carries forward the module docstring's own confirmed-historical-bug context intact: the ore/ice formula's structure/rig/security component was originally guessed as a back-solved additive term before the real live wiki formula was confirmed, and the commonly-quoted "2%/level" Scrapmetal Processing figure is confirmed wrong against the real 55% skill-maxed ceiling. |
| `eve_trader/refining/models.py` | `eve_trader_local/refining/models.py` | The whole file, ported in full rather than split on the local side - same reasoning as `constants.py`'s row above. Only `OreCandidate`/`OreShortlistItem`/`OreOption`/`MineralOption` have a consumer in this port's tests there; `OreShortlistRow` and the Mineral Shopping List shapes (`MineralRequirement`, `OrePurchase`, `DirectMineralPurchase`, `MineralCoverage`, `ShoppingListPlan`) come along for the same "don't re-derive the split later" reason and wait for the modules that will actually build them. | Ported 2026-09-03 on the local side, verbatim. |
| `eve_trader/refining/paste_parser.py` | `eve_trader_local/refining/paste_parser.py` | The whole file, verbatim: `parse_paste`'s 9-column tab-separated grammar (confirmed against evepraisal.com's own open-source `evepaste` reference implementation, per the module's own docstring), its short-split/trailing-empty-column padding, the quantity/volume cleaning regexes, and `merge_duplicate_stacks`' case-insensitive quantity summing. | Ported 2026-09-03 on the local side, verbatim - genuinely pure, storage-free text parsing (no SDE lookups at all; `resolve_type_id`-style name resolution belongs to the not-yet-ported `reprocessing.py` quote module there, one step downstream - see the "File-mapping note" in the reprocessing.py row below). |
| `eve_trader/refining/engine.py` | `eve_trader_local/refining/reprocessing.py`, `eve_trader_local/refining/config.py` | The actual reprocessing-yield math: `ore_ice_base_yield`/`ore_ice_yield` (the real Upwell structure formula - rig points added to a 50-point base, then security and structure modifiers applied multiplicatively to that whole sum, then the Reprocessing/Reprocessing Efficiency/ore-family skills and implant bonus layered on top), `scrapmetal_yield` (the deliberately independent non-ore/ice path - structure/rig/security/implant/the two ore-path skills have no effect there), and `apply_reprocessing_yield`'s two real-EVE roundings (whole-portion batching via `storage.get_portion_size` before any yield is applied, then each output material floored independently rather than the total). **File-mapping note, not a divergence**: this repo's own `refining/reprocessing.py` is actually the *Reprocessing-tab quote calculation* (issue #92 - wraps this same yield math with live ESI/Goonmetrics pricing and a buy-vs-sell recommendation), while the yield math itself lives here in `refining/engine.py`. The local side ports the yield math now (it's pure and has no pricing dependency) and names its file for what's actually in it rather than mirroring this repo's file-name split one commit early; its `reprocessing.py`'s own module docstring documents this explicitly. Added there to support it: `storage.get_portion_size`/`storage.get_type_materials` (both pure reads over `sde_types`/`sde_type_materials`, tables `sde.py` already populates there; this repo's `lru_cache` dropped, same call already made for every other SDE read on the local side) and a new `RefiningConfig` (scope key `"refining"`, loaded/validated exactly like `ProductionConfig`/`DoctrineConfig` there) carrying only the ore/ice+scrapmetal yield fields the ported math reads; `refining_tax_rate` (this repo's own economics field) has no consumer there yet and is deferred with the quote-calculation module that needs it. `cli.py`'s `main()`/`cmd_config` were extended there to call `refining_config.reload()` alongside the existing Trading/Production/Doctrine calls, closing the same "a saved Settings override never actually took effect on the next CLI invocation" gap Doctrine's own port fixed for itself. |
| `eve_trader/refining/pricing.py` | `eve_trader_local/refining/pricing.py` | The Ore Shortlist's per-row profit calculation (issue #91): `landed_cost_per_unit` (Jita sell percentile x (1 + broker fee) + haul cost - the same formula `production/pricing.py`'s own `_candidate_prices` uses for its Jita leg, independently re-derived here since this repo keeps the two tools' pricing modules separate), `mineral_type_ids_for`'s per-candidate-list material union, `_decision`'s Inactive → No market data → Skip/Import precedence (no "Already ordered" tier - the Ore Shortlist has no own-orders tracking), and `evaluate_ore_item`/`evaluate_ore_shortlist`'s portion-size-normalized landed-cost/mineral-value/net-sell/profit/margin math, including a confirmed real bug this file's own comment documents (the profit-vs-`min_profit_threshold` comparison must use the per-*unit* figure, not per-portion, or the threshold is ~portion_size times too lenient). | Ported 2026-09-03 on the local side, near-verbatim. Takes `list[OreCandidate]` as a plain parameter throughout there too - no dependency on `candidate_discovery.py` (not ported, still a "Candidates" row), so it ported cleanly ahead of its own real caller. Uses eve-trader-local's own `.reprocessing` (the yield-math module, see the row above) where this repo imports its own `.engine` - the only import-path adaptation needed. `RefiningConfig.refining_tax_rate` (deferred in the row above) was added there, since this is the first module on the local side that actually reads it. |
| `eve_trader/refining/reprocessing.py` (the real one - see the file-mapping note above) | `eve_trader_local/refining/quote.py` | The Reprocessing-tab quote calculation (issue #92): `resolve_type_id`'s exact-match-only SDE name resolution (deliberately not fuzzy - a paste line's name should already be a real EVE item name), `mineral_type_ids_for_lines`, and `evaluate_reprocessing_line`'s sell-as-is-vs-refined-value comparison including a confirmed real bug this file's own module docstring documents (both options end in a C-J sell order, so both must incur `structure_sell_haircut` - the pre-fix version priced sell-as-is as a bare gross figure, systematically biasing borderline items toward "Sell instead"). Plus the `ReprocessingQuoteRow` dataclass. | Ported 2026-09-03 on the local side, near-verbatim. **Naming decision (three refining-related files now exist across the two repos)**: this repo's own `refining/reprocessing.py` is this quote calculation; eve-trader-local's *own* `refining/reprocessing.py` (see two rows up) already ports this repo's *different* `refining/engine.py` (the pure yield math). Naming the new module `reprocessing.py` too on the local side would collide with a file that already exists there under that name and means something else, so it's named `quote.py` instead there - the live-pricing wrapper around the already-ported yield math, matching this repo's behavior one-for-one, just not its filename. Uses eve-trader-local's own `.reprocessing` module for `apply_reprocessing_yield`/`scrapmetal_yield` where this repo imports its own `.engine` - the only import-path adaptation needed beyond the module rename. **Not yet wired to a CLI command on the local side**: a real "quote a pasted ore list" command needs a live seller-character ESI call (or the Goonmetrics fallback), the same shape `do_quote_reprocessing` has here, which belongs with that tool's own `actions.py`/CLI wiring there (still a "Candidates" row). |
| `eve_trader/refining/optimizer.py` | `eve_trader_local/refining/optimizer.py` | The whole Mineral Shopping List linear program (issue #93): `optimize_shopping_list`'s formulation (one column per candidate ore = whole portions to buy, plus one per directly-buyable mineral = units to buy outright; objective = total landed ISK; one `>=` row per required mineral, `sum_i portions_i x yield_ij + direct_j >= required_j`, negated because `linprog` only speaks `<=`; all variables bounded at `>= 0`), the modelling decisions this file's own module docstring spells out (refining tax modelled as reduced yield rather than an ISK fee, surplus minerals given no credit, and - since the 2026-09-03 fix - portion/direct quantities solved as real integers via `linprog`'s `integrality` param rather than relaxed-and-rounded), and `_repair_shortfalls` (a defensive backstop topping up any mineral with no direct-buy price whose floored yields left it short) plus the final under-coverage assertion. | Ported 2026-09-03 on the local side, verbatim apart from the error type: this file's own `OptimizationError` (converted by its `actions.py`) is dropped there in favour of raising `ActionError` directly, since eve-trader-local keeps that type in `errors.py` precisely so any module can raise it without an import cycle. **Adds `scipy`/`numpy` to eve-trader-local's dependencies** - see the dependency note above. **2026-09-03: fixed a real relax-then-round optimality gap here first, then ported the fix (commit `90c669b`) to the local side** - the old code (both repos) solved a continuous LP relaxation, rounded ore portions UP, then ran a post-hoc `_trim_rounded_plan` heuristic that could only ever *drop* a portion the relaxation had already picked, never *add* one it left at zero, even when that was the true cheapest whole-unit answer (confirmed via brute-force comparison: ~8% of small random cases over the true optimum, worst case 64% over - still always feasible, never under-covering). Fixed by marking BOTH the ore-portion and the direct-mineral-purchase columns as integer via `linprog(..., integrality=...)` (direct-mineral needs it too - leaving it continuous re-opens a smaller version of the same gap) and solving that as the real plan; the old unconstrained continuous solve is kept once, only to report `lp_cost` as a theoretical floor. `_trim_rounded_plan` and its only caller-helper `_plan_cost` are now dead code and were removed from both repos; `_repair_shortfalls` is kept as a defensive backstop against solver float noise or a MIP time-limit fallback, plus a new `_MIP_TIME_LIMIT_SECONDS` (5s) safety net for pathological branch-and-bound cases. Both repos now have real integer programming here, not relax-then-round. |
| `eve_trader/refining/actions.py` | `eve_trader_local/refining/actions.py` | The Ore-&-Minerals-relevant `do_*` subset: `do_add_ore_to_shortlist`/`do_refresh_ore_shortlist`/`do_deactivate_ore_shortlist_items`/`do_activate_ore_shortlist_items`, `do_quote_reprocessing`, `do_list_refinable_minerals`/`do_load_mineral_requirements`/`do_save_mineral_requirements`/`do_optimize_mineral_shopping_list`, and `do_update_settings`. Same thin-orchestration/`ActionError`-boundary shape as this repo's own `actions.py`/`production/actions.py`/`doctrine/actions.py` - reuses Trading's own seller character/C-J structure throughout there too, since there's no Ore-specific login on the local side either. | Ported 2026-09-03 on the local side. Deliberately different from here in one place, confirmed by reading this file's actual call chain rather than assumed: `do_optimize_mineral_shopping_list` here sources its mineral requirements from a saved `mineral_requirements` table (edited by the web UI, or defaulted from it) - not from a Production build's material list, which the module docstring's own reasoning ("the minerals are consumed by Production") could suggest but the real code never wires up. The local side ports that same saved-list default rather than inventing a live Production-BOM link this repo itself doesn't have. `do_get_ore_shortlist` is a small local-only addition with no counterpart here - this repo's web frontend reads `storage.latest_ore_snapshot()` directly, but the CLI needs a `do_*` to print something after a refresh, same precedent as Production's `do_list_stock_targets` there. `do_set_mineral_requirement`/`do_remove_mineral_requirement` are also new on the local side: `do_save_mineral_requirements` here always replaces the whole list (a web editor submits it wholesale), which doesn't fit a one-item-at-a-time CLI command the way `set-stock-target` needs to there - both load the existing saved list, upsert/remove one entry, and re-save through that same replace-all path. Added there to support it: the `ore_shortlist`/`ore_shortlist_snapshot`/`mineral_requirements` SQLite tables (no `tenant_id` - same two-table live-list/append-snapshot shape as Trading's own `shortlist`/`shortlist_snapshot` there) with matching CRUD - raw tuples in/out there, not `refining.models` dataclasses, matching this repo's own "`storage.py` never imports a submodule's own models" precedent. CLI on the local side: `add-ore-to-shortlist`, `refresh-ore-shortlist`, `list-ore-shortlist`, `quote-reprocessing <path>`, `set-mineral-requirement <item> <qty>`/`remove-mineral-requirement <item>`/`list-mineral-requirements`, `solve-shopping-list`. **Ore & Minerals is now fully ported on the local side, end to end.** |
| `eve_trader/refining/candidate_discovery.py` | `eve_trader_local/refining/candidate_discovery.py` | The whole 56-line module: `_family_and_is_ice`'s ore-vs-ice family derivation (ore families come from the SDE group name itself - one "Compressed <Family>" group per family - while every compressed ice variant shares one "Compressed Ice" group, so ice needs its family parsed from the *type* name instead) and `build_ore_candidate_universe`'s straight SDE-row-to-`OreCandidate` mapping. | Ported 2026-09-03 on the local side, verbatim. `pricing.py`'s `evaluate_ore_item`/`evaluate_ore_shortlist` there already took `list[OreCandidate]` as a plain parameter (see the row above), so this ported cleanly as the last piece feeding it. Added there to support it: `storage.load_ore_ice_candidate_types` (the `t.type_name LIKE 'Compressed%'` filter, not `group_name`, is load-bearing per this file's own comment: a compressed ore/ice type shares its *raw* ore's group in the real SDE). No CLI wiring on the local side despite being a pure SDE scan like Trading's `build-universe` - its only real consumer there is the not-yet-ported Ore Shortlist refresh action, and a standalone command with no shortlist to populate would have no use until `actions.py` exists there. |

### Station Trading

A separate tool from Trading/Production/Doctrine/Ore & Minerals here too -
the five share only the SDE/ESI/config foundation. Real mechanic, confirmed
by reading `actions.py`'s module docstring and `candidate_discovery.py`/
`undercut.py` rather than assumed from the name alone: unlike the other
four tools' Jita-import-to-C-J-sell (or buy-ore-at-Jita-refine-and-sell-at-
C-J) model, Station Trading buys and sells *within the same station* -
Jita's own NPC trade hub - profiting from the bid-ask spread itself
(classic EVE "station trading"/market-making), never touching C-J at all.
Fully ported on the local side.

| File here | File in eve-trader-local | What's shared | Status |
|---|---|---|---|
| `eve_trader/station_trading/constants.py` | `eve_trader_local/station_trading/constants.py` | The whole file: the seven real trade-skill type_ids, `SKILL_LABELS`, and `order_slots_from_skills`' base-5-plus-per-skill-per-level order-slot formula. | Ported 2026-09-03 on the local side, verbatim - confirmed no real overlap with `production/constants.py`'s own `job_slots_from_skills` (a different skill tree - industry job slots, not market order slots). |
| `eve_trader/station_trading/config.py` | `eve_trader_local/station_trading/config.py` | The `StationTradingConfig` field definitions: the real Jita trade-hub station_id, the itemized broker-fee/sales-tax rates (charged differently per leg), and the discovery gates (`min_spread_threshold`/`min_daily_volume`) plus the same off-by-default shortlist-cap shape `TradingConfig` already has. | Ported 2026-09-03 on the local side, following the now-established `ProductionConfig`/`DoctrineConfig`/`RefiningConfig` pattern there instead of `ConfigProxy`/contextvars - no enum-style fields, so no extra validation layer. Storage scope key `"station_trading"` there. |
| `eve_trader/station_trading/candidate_discovery.py` | `eve_trader_local/station_trading/candidate_discovery.py` | The whole two-stage discovery shape: `discover_candidates`' single Goonmetrics current-price dump for the entire Jita market, ranked by bid-ask spread gated on `cfg.min_spread_threshold`, then real average daily traded volume (Goonmetrics region history - never order-book depth, same "Theoretical ceiling figures" reasoning every tool's discovery already follows) gated on `cfg.min_daily_volume`, and `confirm_live`'s bounded `region_order_stats_bulk` call against an already-narrowed shortlist, never the whole market. | Ported 2026-09-03 on the local side, verbatim - only the import path changed, since `GoonmetricsClient`/`ESIClient` there already have identical method signatures. |
| `eve_trader/station_trading/undercut.py` | `eve_trader_local/station_trading/undercut.py` | The whole file: `check_undercut_pooled`/`check_buy_undercut_pooled`'s bidirectional own-order-vs-competitor comparison at `cfg.station_id`, pooled across every registered trader character (issue #46's same reasoning `own_orders.py` already carries), and `_check_side`'s `order_id`-only cross-referencing (ESI's order book has no owning-character field). | Ported 2026-09-03 on the local side, verbatim. |
| `eve_trader/station_trading/esi_sync.py` | `eve_trader_local/station_trading/esi_sync.py` | Character registration for the `trader` role: `STATION_TRADING_ROLE_PREFIX`, `STATION_TRADING_SCOPES` and `list_trader_characters`. | Ported 2026-09-03 on the local side. Confirmed by reading the real code: Station Trading does **not** reuse Trading's own `buyer`/`seller` characters - it registers its own distinct `trader` role prefix and scope pair, kept separate the same way Production's `producer` role already is. No `sync_esi()`-style bulk pull there either - own orders and skill levels are read live, on demand. |
| `eve_trader/station_trading/actions.py` | `eve_trader_local/station_trading/actions.py` | The whole `do_*` set: `do_list_trader_characters`/`do_remove_trader_character`, `do_refresh_shortlist`/`do_get_shortlist`, `do_deactivate_shortlist_items`/`do_activate_shortlist_items`, `do_check_undercut`, `do_get_skill_summary`, `do_update_settings`. Plus `_profit`'s real market-mechanic formula: broker fee charged on *both* legs (buy and sell placement) but sales tax only on the sell leg, and `profit_per_day = profit_per_unit x avg_daily_volume` (the same whole-market theoretical-ceiling convention every tool's daily-profit figure already follows). | Ported 2026-09-03 on the local side. `ActionError` comes from eve-trader-local's own `errors.py` there (not this repo's `from ..actions import ActionError` path) - the same import path every other tool's `actions.py` there already uses. Added there to support it: the `station_trading_shortlist` SQLite table (no `tenant_id`, no append-only snapshot counterpart - every row is re-priced live off the current order book on every read there, so no history table is needed) with matching CRUD - the upsert deliberately never touches `active` on conflict, so a re-discovery run can't silently reactivate a deactivated item. CLI there: `auth --role trader`, `refresh-station-shortlist`, `list-station-shortlist`, `check-station-undercut`, `station-trading-skills`. |

## The orchestration layer (`actions.py`) — an integration, not a port

`eve_trader_local/actions.py` (2026-09-02) is a different kind of entry from
every row above: the rows are file-for-file ports of business logic, this is
the *integration* that finally wires them together on the local side. It
follows this repo's own `actions.py` structurally — same `do_*` names, same
"thin orchestration only, real logic stays in the other modules" rule, same
`ActionError` boundary the CLI catches — but it is a synthesis against
eve-trader-local's own modules, so a future diff against this file will
never apply cleanly line-for-line. **Sync its *behavior*, not its text**: a
change to this file's step ordering, pruning policy, failure isolation or a
`do_*`'s documented result shape is a sync candidate; its tenant/config/
DataFrame plumbing is not.

What's covered on the local side: `do_list_buyer_characters`/
`do_list_seller_characters`/`do_remove_trading_character`,
`do_wallet_balance`/`do_wallet_transactions`, `do_update_settings`,
`do_build_universe`/`do_build_focused`, `do_find_new_candidates`,
`do_add_to_shortlist`, `do_refresh_shortlist` (plus the shared
`_refresh_shortlist_rows`/`_backfill_meta_levels`), `do_shortlist_trends`,
`do_check_seller_unlisted_stock`/`do_check_undercut`,
`do_refresh_and_prune_candidates` (with `_items_past_skip_grace_period`/
`_items_beyond_rank`/`_items_to_reactivate` and
`shortlist_skip_deactivation_days`), `do_reconcile_trades` and
`do_pipeline`. This file's two real behavioral lessons came over in the
code there: `do_pipeline` calls `do_refresh_and_prune_candidates`, never a
separate find+refresh pair (this repo's "Run Complete Pipeline" once added
and pruned nothing at all because of that), and each of its steps is
isolated in its own try/except so a missing login or a network hiccup in
one can't stop the others.

Deliberately excluded on the local side: `do_create_backup`/
`do_list_backups` (this repo's `backup.py` shells out to
`docker exec pg_dump` — see "Never sync"), and anything tenant/admin-shaped.
`do_auth` is excluded too: eve-trader-local's `cli.py` `auth` command calls
`TokenManager.login` directly, and its TokenManager has only ever stored
characters under `f"{prefix}:{character_id}"`, so this repo's legacy-
single-key reconciliation in `_list_role_characters` has nothing to
reconcile there — it is one `tm.list_records(prefix)` call.
`do_recategorize_shortlist` and `do_list_transaction_characters` are left
out there as web-UI-specific one-offs (a one-time fix-up for a bug the
local shortlist never had, and a character picker for a tab that doesn't
exist); port them if a GUI needs them. `do_wallet_transactions` returns raw
`location_id`s on the local side where this repo resolves display names —
that needs this repo's persisted `structure_names` table, which
eve-trader-local has no equivalent of yet.

Added on the local side specifically to support this layer:
- `storage.py`: the `new_candidates`, `goonmetrics_history`,
  `candidate_search_cursor` and `shortlist_skip_streak` tables (the
  persistence `candidate_discovery.py`/`history_backtest.py` were
  deliberately ported without, since their caller owns it there) with
  matching save/load helpers, plus `sde_type_names`. Reads there return
  `NewCandidateResult`/`HistoryPoint` objects where this repo returns
  DataFrames (no pandas on the local side) — the history read imports
  `HistoryPoint` *inside* the function, since a module-level import would
  close a `storage -> goonmetrics_client -> config -> storage` cycle there.
  `new_candidates` is append-only there (a safe-mode run only ever covers a
  rotating window); reads filter to `MAX(run_ts)` themselves.
- `config.py`: `TradingConfig.skip_grace_period_days`,
  `enforce_shortlist_cap` and `max_active_shortlist_items` on the local
  side — the pruning policy fields the `shortlist.py` row deferred to
  exactly this layer, with `_FIELD_RANGES` entries for the two numeric ones.
- `cli.py`: `build-universe`, `find-candidates` (`--safe/--full`, matching
  this repo's own flag pair), `add-to-shortlist`, `refresh-shortlist`,
  `check-unlisted-stock`, `check-undercut`, `reconcile-trades` and
  `pipeline` (`--rebuild-universe`) on the local side. One command per
  action, printing only — no logic, so a future GUI calls the same `do_*`
  functions.

One deliberate behavioral difference from this repo, not an oversight:
eve-trader-local's `do_add_to_shortlist` raises only when *no* candidate
search has ever run, and returns `{"added": 0}` when the latest run simply
recommended nothing. This repo raises on an empty table only, which amounts
to the same thing here (its table accumulates across tenants' runs); there
the distinction has to be explicit so a normal "nothing worth adding this
run" outcome doesn't abort `do_refresh_and_prune_candidates` before it
prunes.

## Candidates — port when the algorithm changes, once each exists on the local side

None of these are in `eve-trader-local` yet. When one gets ported over as a
real local feature, add it to the table above and keep the following in
sync going forward. Each entry names *what part* is shareable — usually the
computation, not the surrounding storage/config wiring:

| File | Shareable logic |
|---|---|
| `eve_trader/production/engine.py`'s `logistics_status`/`distribution_recommendations`, `_direct_material_mult` | The multi-structure Logistik-tab helpers - judged genuinely out of scope for a single-structure local install, not deferred (see the `engine.py` row above for why: this repo's own per-category structure-assignment model has nothing on the local side for them to attach to). Everything else in Production's planner pairing is now ported - see the Production section above |
| `eve_trader/models.py` (the rest), `eve_trader/*/models.py` | Plain dataclasses — portable as-is where they don't reference `storage`/`TRADING_CONFIG` directly. `Candidate`, `NewCandidateResult`, `ShortlistItem`, `ShortlistRow`, `RealizedTrade`, `UnlistedStockRow` and `UndercutRow` are ported; the rest belong to modules still listed here, so each arrives with its own |
| `TradingConfig`/`ProductionConfig` **field definitions** (`config.py`, `production/config.py`) | The behavioral fields (region ids, thresholds, economics) — not the `ConfigProxy`/contextvars machinery around them |

## Never sync (intentionally divergent, or not applicable)

- `storage.py`, `tenant_scope.py` — Postgres/RLS is the whole reason
  eve-trader-local exists as a separate repo, not a fork.
- `access_gate.py`, `admin.py` — multi-tenant/cross-tenant concepts with no
  local-single-user equivalent.
- `api/*` — FastAPI web layer; eve-trader-local's target UI is a native GUI,
  not a web frontend (`frontend/*` doesn't apply either).
- `scheduler.py` — per-tenant background-job iteration; a local app's
  "scheduler" (if it ever needs one) would be a single-user timer, not
  worth sharing code with this.
- `backup.py` — shells out to `docker exec pg_dump`; meaningless without
  Postgres.
- `sqlite_migration.py` — a one-time Postgres *cutover* tool; conceptually
  the opposite direction from eve-trader-local's SQLite-native storage.
- Any tenant's or local user's own data/settings — this file tracks code,
  never data.

## Maintenance

Update this table (both copies — see the mirrored `SYNC.md` in
eve-trader-local) whenever:
- A "candidate" module actually gets ported to eve-trader-local → move its
  row to "Already ported".
- A genuinely new, storage/tenant-agnostic business-logic module is added
  here → add it to "Candidates".
- A module you'd expect to share turns out to be too entangled with
  storage/config to port cleanly → note it under "Never sync" with why,
  so nobody re-attempts the same porting effort later.
